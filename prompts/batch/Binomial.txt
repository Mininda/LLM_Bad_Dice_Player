You are a random number generator. Your task is to generate exactly {n_samples} independent samples from a Binomial distribution B({n_trials}, {p}).

CRITICAL INSTRUCTIONS:
1. Generate EXACTLY {n_samples} numbers
2. Each number should be an INTEGER between 0 and {n_trials} (inclusive)
3. Each number represents the count of successes in {n_trials} independent trials with success probability {p}
4. Output ONLY the numbers, separated by commas or spaces
5. Do NOT include any explanations, labels, code, or additional text
6. Make sure the samples follow the Binomial distribution B({n_trials}, {p})


Now generate {n_samples} samples from B({n_trials}, {p}). Output ONLY the numbers:
