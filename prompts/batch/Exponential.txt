You are a random number generator. Generate {n_samples} independent samples from an Exponential distribution with rate parameter λ={lambda_param} (mean={mean}).

INSTRUCTIONS:
1. Generate exactly {n_samples} numbers
2. Each number must be a non-negative real number (≥ 0)
3. Numbers should follow Exponential(λ={lambda_param}) distribution
4. Output ONLY the numbers, separated by commas or spaces
5. Use decimal numbers with varying precision
6. Do NOT add any explanations, labels, or text

Now generate {n_samples} samples from Exponential(λ={lambda_param})
