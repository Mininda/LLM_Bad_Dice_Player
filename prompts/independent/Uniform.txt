Generate exactly ONE random number from a Uniform distribution on the interval [{low}, {high}].

INSTRUCTIONS:
1. Output ONLY one single number
2. This should be an independent random sample from U({low}, {high})
3. Do NOT include any explanations, labels, or additional text
4. Just output the number with decimal precision

Output:
